// ============================================================
// Remotion Entrypoint
// ============================================================

import { registerRoot } from 'remotion';
import { Root } from './Root';
import './styles/fonts.css';

registerRoot(Root);
